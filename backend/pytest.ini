[pytest]
# Make the `app` package importable when running pytest from backend/. The `tests/` dir is
# added to sys.path automatically (no __init__.py), so `from conftest import …` resolves.
pythonpath = .
testpaths = tests
addopts = -ra
