# Dev/test dependencies (not needed at runtime). Install on top of requirements.txt:
#   pip install -r requirements.txt -r requirements-dev.txt
pytest>=8.0
# httpx is also a runtime dep (Storage) but TestClient needs it too; pinned in requirements.txt.
