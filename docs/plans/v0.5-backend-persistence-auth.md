# v0.5 — Full backend + persistence + auth (implementation plan)

- **Status:** In progress — Phases 0–2 done (committed `972aec6`, pushed). Resuming at the
  **Phase 1 live-DB migration**.
- **Date:** 2026-06-17
- **Decisions:** [ADR 0007](../adr/0007-backend-persistence-and-auth.md) — SQLAlchemy/Alembic on
  Supabase Postgres; backend-mediated Supabase JWT; RLS as defense-in-depth.

## ⏯ Resume here (state as of 2026-06-17)

**Done & pushed (commit `972aec6`):** Phase 0 (DB/Alembic scaffold), Phase 1 (8 ORM models +
migrations `0001` portable / `0002` Supabase auth FKs + RLS — *written & import-verified, not yet
run against a DB*), Phase 2 (Supabase JWT auth, `X-Cram-Secret` retired — verified).

**Supabase project:** created. Recommended security toggles: Data API **off**, auto-expose **off**,
automatic RLS **on**. Private Storage bucket `sources` to be created (confirm).

**Local `backend/.env`** (gitignored — exists only on the dev machine): the v0.5 key scaffold is
appended but **values not yet filled**. Need at least `DATABASE_URL` (transaction pooler, 6543) and
`DATABASE_DIRECT_URL` (direct 5432, or session pooler if IPv6 fails) from the dashboard **Connect**
button. Auth/Storage keys (`SUPABASE_URL`, `SUPABASE_JWKS_URL` *or* `SUPABASE_JWT_SECRET`,
`SUPABASE_SERVICE_ROLE_KEY`) can be added when wiring Phase 3.

**Exact next steps tomorrow:**
1. Fill the two `DATABASE_*` URLs in `backend/.env`.
2. From `backend/`: `alembic upgrade head` → builds all 8 tables + auth FKs/RLS.
3. `alembic revision --autogenerate -m "drift check"` → expect an **empty** migration (delete it if
   so); reconcile any drift against `app/models/`. ← *closes Phase 1 acceptance.*
4. Confirm `/healthz` reports `"db": "ok"`.
5. Begin **Phase 3** (CRUD + sync endpoints + persisting generation/grading output to DB + Storage).

**Open housekeeping (non-blocking):** fold the two ADR 0007 refinements back into the ADR text
(`storage_paths` is a list, not singular; `exam_date`/`due_date` are `timestamptz`); create
`docs/ROADMAP.md`.

## Goal

Turn the stateless v0.4 backend into the system of record: the full data model in Supabase
Postgres, per-user accounts via Supabase Auth, uploaded files in Supabase Storage, and a sync
contract the iOS app targets later. Ship the carried pre-deploy hardening. **All Windows-doable**
except the iOS sync client (Phase 5, Mac).

## Scope boundary

- **In:** backend schema + migrations, auth verification, CRUD + sync endpoints, persisting
  generation/grading output, Storage uploads, hardening.
- **Out (deferred):** the iOS sync client (Mac/Xcode, Phase 5 below), the v0.4 iOS UI work, the web
  dashboard (v0.6).

## Prerequisite — owner does this once in the Supabase dashboard

Not code; needed before Phase 0 can connect:

1. Create a Supabase project; note the **direct** (5432) and **pooler** (6543) connection strings.
2. Grab the **service-role key**, the **project URL**, and the **JWT secret** (or note the JWKS URL
   `…/auth/v1/.well-known/jwks.json`).
3. Create a **private** Storage bucket (e.g. `sources`).
4. Enable email/password auth.

These go into the gitignored `backend/.env` (and `.env.example` gets the keys, no values).

## Phases

### Phase 0 — DB plumbing & config ✅ (done, `972aec6`)
- Add deps: `sqlalchemy`, `alembic`, `psycopg[binary]`, `pyjwt[crypto]`.
- New env vars in `config.py` (`Settings`): `database_url`, `supabase_url`, `supabase_jwt_secret`
  / `supabase_jwks_url`, `supabase_service_role_key`, `supabase_storage_bucket`.
- `app/db.py`: SQLAlchemy engine + session factory + FastAPI session dependency. Pooler-safe engine
  options; migrations use the direct URL.
- Alembic skeleton (`backend/alembic/`, `alembic.ini`), env wired to `Settings.database_url`.
- `/healthz` gains a DB connectivity check (`SELECT 1`).
- **Acceptance:** app boots, `/healthz` reports DB reachable, `alembic upgrade head` runs clean
  (empty migration).

### Phase 1 — Data model + initial migration 🟡 (models done `972aec6`; live-DB migration pending)
- SQLAlchemy models for all tables in ADR 0007 §4 (`app/models/`), UUID PKs, `created_at`,
  `updated_at`, `deleted_at` where specified; enums matching existing iOS/contract values.
- First Alembic migration creating every table + indexes (`user_id`, `subject_id`, `updated_at`).
- RLS policies as a migration step (SQL): enable RLS, `auth.uid()` policies — defense-in-depth.
- **Acceptance:** `alembic upgrade head` builds the full schema on the Supabase Postgres; downgrade
  works; a smoke test inserts/reads one row per table via the ORM.

### Phase 2 — Auth (backend-mediated JWT) ✅ (done, `972aec6`)
- `app/auth.py`: `get_current_user` dependency — verify Supabase JWT (JWKS or shared secret),
  extract `sub`, return a `CurrentUser`. 401 on missing/invalid/expired.
- Apply it to data endpoints; move `/v1/generate` and `/v1/grade` under JWT (ADR 0007 §3). Retire
  `X-Cram-Secret` from the client path (optionally keep an internal secret for system calls).
- Keep the `CRAM_ENV=prod` fail-closed guard; extend it to require auth config in prod.
- **Acceptance:** a valid Supabase test JWT is accepted and scopes queries to that user; a missing/
  forged/expired token is rejected 401; cross-user access is impossible (ownership-scoped queries).

### Phase 3 — CRUD + sync API + persisting AI output
- REST endpoints per resource (subjects, sources, cards, quizzes, questions, attempts,
  grade_entries, review_logs), all ownership-scoped (`user_id == current_user`).
- Sync primitives (ADR 0007 §5): `GET …?since=<cursor>` delta pull incl. tombstones; batch upsert
  by client UUID; append-only insert for `attempts` / `review_logs`.
- `/v1/generate`: persist the `source` (file → Storage, `storage_path` recorded) and the generated
  `cards`/`questions` under the user. `/v1/grade`: persist the `attempt` (incl. `feedback`).
- **Acceptance:** end-to-end pytest — create subject → generate (rows + file persisted) → pull
  delta → grade (attempt persisted) → soft-delete → tombstone appears on next pull. Cross-user
  isolation asserted.

### Phase 4 — Pre-deploy hardening (carried O1 / M1 / prod)
- **O1:** per-user/IP rate limit on `/v1/*`; **Anthropic spend cap** (daily/monthly token-or-cost
  ceiling, refuse over-budget with a clear error; logged).
- **M1:** documented hard **reverse-proxy body cap** (in front of the app-level cap in `main.py`).
- **prod:** `CRAM_ENV=prod` checklist — auth required, secrets set, debug off; extend the startup
  guard to assert prod config completeness.
- **Acceptance:** rate limit returns 429 past threshold; spend cap blocks past budget; deploy
  checklist documented in `docs/SETUP.md`; prod boot fails fast on missing config.

### Phase 5 — iOS sync client *(Mac/Xcode — not this session)*
- Cache decks locally (SwiftData), queue reviews/grades offline, sync against the Phase 3 contract;
  define offline UX. May be promoted to its own ADR (PRODUCT-SPEC §9 open question).

## Testing
- `pytest` against a disposable Postgres (a dedicated Supabase test project or a local Postgres
  matching the migration). Auth tests mint test JWTs with the project secret / a test keypair.
- Run `alembic upgrade head` in CI-style check before the endpoint tests.

## Sequencing note
Phases 0→4 are strictly ordered (schema before auth before endpoints before hardening). Within a
phase, model definitions and migrations can be drafted in parallel with the endpoint scaffolding,
but nothing merges until its phase acceptance passes.

## Docs to update on completion
- README status table (Backend → persistence + auth live).
- `docs/SETUP.md` (Supabase env vars, migration commands, prod deploy checklist).
- `docs/ARCHITECTURE.md` if the auth/data path diagram needs the JWT flow called out.
